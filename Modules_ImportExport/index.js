const {add , sub} = require("./math")       // require() function is used to import the built-in packages or modules

console.log(`Math value is ${add(5,5)}`);

console.log(`Math value is ${sub(10,2)}`);  

